version: '3.8'
services:
  jupyter:
      build:
        context: ./jupyter
        dockerfile: Dockerfile
      volumes:
        - ./jupyter:/app/
      ports:
        - 8888:8888
      depends_on:
        - clickhouse
        - postgres
  clickhouse:
    image: clickhouse/clickhouse-server
    user: "101:101"
    container_name: clickhouse
    hostname: clickhouse
    volumes:
      - ./configs/clickhouse/clickhouse-server/config.d/config.xml:/etc/clickhouse-server/config.d/config.xml
      - ./configs/clickhouse/clickhouse-server/users.d/users.xml:/etc/clickhouse-server/users.d/users.xml
      - ./configs/clickhouse/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    ports:
      - "127.0.0.1:8123:8123"
      - "127.0.0.1:9000:9000"
    depends_on:
      - postgres
  postgres:
    container_name: postgres
    hostname: postgres
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: clickhouse_pg_db
    ports:
      - "5432:5432"
    volumes:
      - ./configs/clickhouse/postgres/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    command:
      - "postgres"
      - "-c"
      - "wal_level=logical" #required for MaterializedPostgreSQL
